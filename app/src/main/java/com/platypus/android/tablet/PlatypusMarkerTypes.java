package com.platypus.android.tablet;

/**
 * enum that defines the different markers that can appear on the map
 * <p>
 *     This is used to determine what appears in the marker info window
 * </p>
 */
public enum PlatypusMarkerTypes
{
		VEHICLE,
		WAYPOINT,
		HOME,
		BREADCRUMB,
		SENSORDATA
}
